import { useEffect } from 'react';

const TransferData: React.FC<{}> = () => {
  useEffect(() => {
    //
  });

  return (
    <p>Please be patient while we automagically transfer your map data...</p>
  );
};

export default TransferData;
